<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="czopekonieszczuk.scalatomato"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:label="@string/app_name" >
        <activity android:name=".activities.LoginActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RegisterActivity"
            android:label="@string/register_text" />
        <activity android:name=".activities.UserActivity"
            android:label="@string/user_activity_name"/>
        <activity android:name=".activities.UserTomatoesActivity"
            android:label="@string/user_tomatoes_activity_name"/>
        <activity android:name=".activities.TomatoActivity"
            android:label="@string/tomato_activity_name"/>
    </application>
</manifest>
